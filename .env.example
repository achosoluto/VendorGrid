# Database Configuration
# For local development, you can use a local PostgreSQL instance or Neon serverless
# Example: postgresql://username:password@localhost:5432/vendorgrid
# For Neon: postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/vendorgrid
DATABASE_URL=postgresql://localhost:5432/vendorgrid

# Session Secret - Generate a random string for session encryption
# Example: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here-change-this-in-production

# Replit Auth Configuration (Required for Replit deployment)
# These are automatically provided by Replit, but needed for production-like testing
REPL_ID=your-repl-id-here
REPLIT_DOMAINS=your-domain.replit.dev
ISSUER_URL=https://replit.com/oidc

# Local Development Mode (Set to 'true' to bypass Replit auth for local testing)
# When enabled, uses mock authentication with in-memory sessions
LOCAL_DEV_MODE=true

# Server Port (Default: 5000)
PORT=5000
