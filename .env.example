# =============================================================================
# VENDORGRID ENVIRONMENT CONFIGURATION
# =============================================================================
# 
# This file shows all available environment variables for VendorGrid.
# Copy this file to .env and configure as needed.
#
# AUTHENTICATION PROVIDER CONFIGURATION
# =============================================================================
# 
# Select authentication provider: "mock" (default) or "keycloak"
# - mock: Use mock authentication (development/testing)
# - keycloak: Use Keycloak OIDC authentication (production)
AUTH_PROVIDER=mock

# =============================================================================
# MOCK AUTHENTICATION (Development)
# =============================================================================
# 
# Mock authentication is used for development and testing
# No additional configuration needed for mock auth

# =============================================================================
# KEYCLOAK AUTHENTICATION (Production)
# =============================================================================
# 
# Required for production authentication with Keycloak
# These should be configured when switching to keycloak provider

# Keycloak Server Configuration
KEYCLOAK_BASE_URL=http://localhost:8080
KEYCLOAK_REALM=vendorgrid
KEYCLOAK_CLIENT_ID=vendorgrid-app

# Client Secret (generated in Keycloak Admin Console)
# Get this from: Keycloak Admin → vendorgrid realm → Clients → vendorgrid-app → Credentials
KEYCLOAK_CLIENT_SECRET=REPLACE_WITH_GENERATED_CLIENT_SECRET

# Alternative: Admin Client (for server-side operations)
KEYCLOAK_ADMIN_CLIENT_SECRET=REPLACE_WITH_GENERATED_ADMIN_CLIENT_SECRET

# Callback URL (optional, defaults to /api/callback)
KEYCLOAK_CALLBACK_URL=/api/callback

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# 
# PostgreSQL database connection
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vendorgrid

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================
# 
# Session secret for Express session encryption
# Use a strong, random string in production
SESSION_SECRET=your-session-secret-key-change-in-production

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# 
# Application environment (development, production, test)
NODE_ENV=development

# Application port
PORT=3000

# =============================================================================
# MIGRATION NOTES
# =============================================================================
# 
# To switch from mock to keycloak authentication:
# 1. Set AUTH_PROVIDER=keycloak
# 2. Configure KEYCLOAK_* variables above
# 3. Generate client secrets in Keycloak
# 4. Restart the application
#
# To rollback from keycloak to mock:
# 1. Set AUTH_PROVIDER=mock
# 2. Restart the application
# 3. The previous mock authentication will be restored